# TODO — 미구현 기능 목록

---

## 버그 (Bug)

| 항목 | 파일 | 설명 |
|------|------|------|
| **일정 등록 시 사용자 오류** | `CreateEventModal.tsx:82` | `me = users[0]` 으로 고정되어 있어 개인 일정이 항상 첫 번째 사용자 ID로 등록됨. `currentUser`로 교체 필요 |

---

## 일정 등록 모달 (`CreateEventModal`)

| 항목 | 설명 |
|------|------|
| **회사 일정 타입 선택** | 생성/수정 시 `type`이 `'event'`로 하드코딩. '미팅', '공휴일', '이벤트' 선택 UI 없음 |
| **회사 일정 시간/장소 입력** | `time`, `location` 입력 필드 없음. 현재 생성 시 API에도 전달되지 않음 |
| **어드민 타 사용자 일정 등록** | 어드민이 다른 사용자의 연차/반차를 대신 등록하는 기능 없음 (사용자 선택 UI 필요) |

---

## 팀 관리 (`/teams`)

| 항목 | 설명 |
|------|------|
| **팀원 제거** | 팀 상세 > 팀원 테이블에서 팀에서 제거하는 버튼 없음. 현재 배정(assign)만 가능 |

---

## 인증 / 계정

| 항목 | 파일 | 설명 |
|------|------|------|
| **비밀번호 찾기** | `LoginPage.tsx` | 로그인 페이지에 "비밀번호를 잊으셨나요?" 링크/기능 없음 |
| **비밀번호 변경** | — | 로그인 후 본인 비밀번호를 변경하는 UI/API 없음 |
| **회원가입 역할 선택** | `SignupPage.tsx` (Step 3) | 가입 완료 시 역할이 '사원'으로 고정. 역할 선택 UI 없음 |

---

## 공통 UI

| 항목 | 파일 | 설명 |
|------|------|------|
| **Topbar 검색 버튼** | `Topbar.tsx:21` | 버튼 렌더링만 되어 있고 클릭 액션 없음 |
| **Topbar 알림 버튼** | `Topbar.tsx:24` | 버튼 렌더링만 되어 있고 클릭 액션 없음 |

---

## 내 프로필

| 항목 | 설명 |
|------|------|
| **프로필 페이지** | 본인 이름·팀·역할을 확인하고 수정하는 전용 페이지 없음. Sidebar에 이름만 표시됨 |

---

## 완료된 항목

### 팀 관리 (`/teams`) ✅
- ~~AssignMembersModal — 체크박스로 다중 선택 후 팀 배정~~
- ~~EditMemberModal — 이름·팀·역할 편집~~

### 일정 등록 모달 (`CreateEventModal`) ✅
- ~~외근/출장 유형 등록 (`type: 'trip'` PersonalEvent로 등록)~~

### 인증 (`/`, `/signup`) ✅
- ~~로그인 API 연동 (`POST /api/auth/login`)~~
- ~~회원가입 3단계 API 연동~~
- ~~이메일 인증 코드 발송 (nodemailer SMTP)~~
- ~~로그아웃 (localStorage 정리 + AppContext 초기화)~~
- ~~인증 상태 지속 (JWT localStorage, 401 자동 로그아웃)~~

### 캘린더 (`/calendar`) ✅
- ~~날짜 클릭 — 일정 등록 (해당 날짜로 모달 오픈)~~
- ~~이벤트 칩 클릭 — 상세 팝오버~~
- ~~+N개 더보기 팝오버~~

### 대시보드 (`/dashboard`) ✅
- ~~다가오는 일정 클릭 — 상세 팝오버~~

### 공통 ✅
- ~~일정 수정/삭제 (팝오버 수정·삭제 버튼, CreateEventModal edit 모드)~~
- ~~관리자 권한 DB 기반 연동 (`is_admin` 컬럼)~~
- ~~사용자 관리 — 관리자 권한 토글~~
